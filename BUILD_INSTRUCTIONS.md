# РРЅСЃС‚СЂСѓРєС†РёСЏ РїРѕ СЃР±РѕСЂРєРµ PackMate APK

## Р‘С‹СЃС‚СЂС‹Р№ СЃС‚Р°СЂС‚

### РЁР°Рі 1: РЈСЃС‚Р°РЅРѕРІРёС‚Рµ Flutter SDK

**Windows:**
```powershell
# РЎРєР°С‡Р°Р№С‚Рµ Flutter SDK
Invoke-WebRequest -Uri "https://storage.googleapis.com/flutter_infra_release/releases/stable/windows/flutter_windows_3.24.5-stable.zip" -OutFile "flutter.zip"

# Р Р°СЃРїР°РєСѓР№С‚Рµ РІ C:\flutter
Expand-Archive -Path flutter.zip -DestinationPath C:\

# Р”РѕР±Р°РІСЊС‚Рµ РІ PATH
$env:PATH += ";C:\flutter\bin"
[Environment]::SetEnvironmentVariable("PATH", $env:PATH, [EnvironmentVariableTarget]::User)

# РџСЂРѕРІРµСЂСЊС‚Рµ СѓСЃС‚Р°РЅРѕРІРєСѓ
flutter doctor
```

**macOS:**
```bash
brew install flutter
flutter doctor
```

**Linux:**
```bash
sudo snap install flutter --classic
flutter doctor
```

### РЁР°Рі 2: РџРµСЂРµР№РґРёС‚Рµ РІ РїР°РїРєСѓ РїСЂРѕРµРєС‚Р°

```bash
cd "C:\\Users\\yappa\\OneDrive\\Desktop\\oh my opencode"
```

### РЁР°Рі 3: РЈСЃС‚Р°РЅРѕРІРёС‚Рµ Р·Р°РІРёСЃРёРјРѕСЃС‚Рё

```bash
flutter pub get
```

### РЁР°Рі 4: РЎРіРµРЅРµСЂРёСЂСѓР№С‚Рµ РєРѕРґ (Isar, Riverpod)

```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

### РЁР°Рі 5: РЎРѕР±РµСЂРёС‚Рµ APK

**Debug APK (РґР»СЏ С‚РµСЃС‚РёСЂРѕРІР°РЅРёСЏ):**
```bash
flutter build apk --debug
```

**Release APK (РґР»СЏ РїСѓР±Р»РёРєР°С†РёРё):**
```bash
flutter build apk --release
```

### РЁР°Рі 6: РќР°Р№РґРёС‚Рµ APK

APK С„Р°Р№Р» РЅР°С…РѕРґРёС‚СЃСЏ РІ:
```
build/app/outputs/flutter-apk/app-debug.apk
# РёР»Рё
build/app/outputs/flutter-apk/app-release.apk
```

## РЈСЃС‚Р°РЅРѕРІРєР° РЅР° С‚РµР»РµС„РѕРЅ

### РЎРїРѕСЃРѕР± 1: USB
1. РџРѕРґРєР»СЋС‡РёС‚Рµ С‚РµР»РµС„РѕРЅ Рє РєРѕРјРїСЊСЋС‚РµСЂСѓ
2. Р’РєР»СЋС‡РёС‚Рµ СЂРµР¶РёРј СЂР°Р·СЂР°Р±РѕС‚С‡РёРєР° Рё USB-РѕС‚Р»Р°РґРєСѓ
3. Р’С‹РїРѕР»РЅРёС‚Рµ: `flutter install`

### РЎРїРѕСЃРѕР± 2: Р¤Р°Р№Р» APK
1. РЎРєРѕРїРёСЂСѓР№С‚Рµ APK РЅР° С‚РµР»РµС„РѕРЅ
2. РћС‚РєСЂРѕР№С‚Рµ С„Р°Р№Р» РЅР° С‚РµР»РµС„РѕРЅРµ
3. Р Р°Р·СЂРµС€РёС‚Рµ СѓСЃС‚Р°РЅРѕРІРєСѓ РёР· РЅРµРёР·РІРµСЃС‚РЅС‹С… РёСЃС‚РѕС‡РЅРёРєРѕРІ
4. РЈСЃС‚Р°РЅРѕРІРёС‚Рµ

## РљРѕРЅС„РёРіСѓСЂР°С†РёСЏ API (РѕРїС†РёРѕРЅР°Р»СЊРЅРѕ)

Р”Р»СЏ СЂР°Р±РѕС‚С‹ РїРѕРіРѕРґС‹ РїСЂРё СЃР±РѕСЂРєРµ:
```bash
flutter build apk --dart-define=WEATHER_API_KEY=РІР°С€_РєР»СЋС‡
```

Р‘РµСЃРїР»Р°С‚РЅС‹Р№ РєР»СЋС‡: https://www.weatherapi.com/signup.aspx

## Р РµС€РµРЅРёРµ РїСЂРѕР±Р»РµРј

### "flutter: command not found"
Р”РѕР±Р°РІСЊС‚Рµ Flutter РІ PATH РёР»Рё РёСЃРїРѕР»СЊР·СѓР№С‚Рµ РїРѕР»РЅС‹Р№ РїСѓС‚СЊ:
```bash
C:\flutter\bin\flutter.bat build apk
```

### РћС€РёР±РєРё build_runner
```bash
flutter clean
flutter pub get
flutter pub run build_runner build --delete-conflicting-outputs
```

### РћС€РёР±РєР° "No connected devices"
```bash
flutter devices
# Р•СЃР»Рё РїСѓСЃС‚Рѕ - РїРѕРґРєР»СЋС‡РёС‚Рµ С‚РµР»РµС„РѕРЅ РёР»Рё Р·Р°РїСѓСЃС‚РёС‚Рµ СЌРјСѓР»СЏС‚РѕСЂ
```

## РЎС‚СЂСѓРєС‚СѓСЂР° РіРѕС‚РѕРІРѕРіРѕ РїСЂРёР»РѕР¶РµРЅРёСЏ

РџРѕСЃР»Рµ СЃР±РѕСЂРєРё РїСЂРёР»РѕР¶РµРЅРёРµ СЃРѕРґРµСЂР¶РёС‚:
- вњ… 9 СЌРєСЂР°РЅРѕРІ (РѕРЅР±РѕСЂРґРёРЅРі в†’ Р·Р°РІРµСЂС€РµРЅРёРµ)
- вњ… Р“РµРЅРµСЂР°С†РёСЏ СЃРїРёСЃРєР° РїРѕ С‚РёРїСѓ РїРѕРµР·РґРєРё
- вњ… Р РµРґР°РєС‚РёСЂРѕРІР°РЅРёРµ (РґРѕР±Р°РІР»РµРЅРёРµ/СѓРґР°Р»РµРЅРёРµ РІРµС‰РµР№)
- вњ… Р РµР¶РёРј СЃР±РѕСЂРѕРІ СЃ РїСЂРѕРіСЂРµСЃСЃРѕРј
- вњ… РЎРѕС…СЂР°РЅРµРЅРёРµ С€Р°Р±Р»РѕРЅРѕРІ
- вњ… РРЅС‚РµРіСЂР°С†РёСЏ РїРѕРіРѕРґС‹ (РѕРїС†РёРѕРЅР°Р»СЊРЅРѕ)

## РўРµСЃС‚РѕРІС‹Рµ СЃС†РµРЅР°СЂРёРё

1. **РџРѕС…РѕРґ РІ РіРѕСЂС‹**
   - Р’С‹Р±СЂР°С‚СЊ "РџРѕС…РѕРґ"
   - РњРµСЃС‚Рѕ: РљР°РІРєР°Р·
   - 5 РґРЅРµР№, РїР°Р»Р°С‚РєР°
   - РџСЂРѕРІРµСЂРёС‚СЊ СЃРЅР°СЂСЏР¶РµРЅРёРµ Рё Р°РїС‚РµС‡РєСѓ

2. **РџР»СЏР¶РЅС‹Р№ РѕС‚РґС‹С…**
   - Р’С‹Р±СЂР°С‚СЊ "РџР»СЏР¶"
   - РњРµСЃС‚Рѕ: РўСѓСЂС†РёСЏ
   - 7 РґРЅРµР№, РѕС‚РµР»СЊ
   - РџСЂРѕРІРµСЂРёС‚СЊ РєСѓРїР°Р»СЊРЅРёРє Рё СЃРѕР»РЅС†РµР·Р°С‰РёС‚Сѓ

3. **РљРѕРјР°РЅРґРёСЂРѕРІРєР°**
   - Р’С‹Р±СЂР°С‚СЊ "РљРѕРјР°РЅРґРёСЂРѕРІРєР°"
   - РњРµСЃС‚Рѕ: РњРѕСЃРєРІР°
   - 3 РґРЅСЏ, РѕС‚РµР»СЊ
   - РџСЂРѕРІРµСЂРёС‚СЊ РґРµР»РѕРІСѓСЋ РѕРґРµР¶РґСѓ Рё С‚РµС…РЅРёРєСѓ

